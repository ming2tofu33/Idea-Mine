export function PositioningBand() {
  return (
    <section
      id="positioning-band"
      aria-label="Positioning statement"
      className="relative z-10 readable-container py-4 md:py-5"
    >
      <div className="observatory-panel observatory-frame rounded-3xl p-5 sm:p-6 lg:p-7">
        <div className="max-w-3xl space-y-3">
          <p className="text-[10px] uppercase tracking-[0.24em] text-cold-cyan/80">
            Positioning
          </p>
          <h2 className="text-3xl font-semibold tracking-tight text-text-primary sm:text-4xl">
            Not just idea generation.
          </h2>
          <p className="max-w-2xl text-sm leading-relaxed text-text-secondary sm:text-base">
            IDEA MINE keeps the thread from discovery to decision, so the best
            signals become durable, reviewable product direction.
          </p>
        </div>
      </div>
    </section>
  );
}
